# KIT-0091 — Review Starter

**PRs**: #112 (preflight + ghio) → #113 (review_input + worktree +
target_repo + F5, 0.2.0), stacked — #113 retargets to `main` after
#112 merges (STACKED-PR-WORKFLOW reconciliation).
**Branches**: `feature/KIT-0091-port-gate-scripts` /
`…-pr2` (worktree `~/Github/ask-worktrees/KIT-0091`)
**Scope**: the whole task — phase 1b of KIT-ADR-0028. All 1,761
measured bash lines of the three gate surfaces now live in
`agentive-kit`; the scripts are one-release deprecation shims.

## Gate status

**Snapshot**: 2026-08-07, heads `2563a4c` (#112) / `f873a2e` (#113).

- **Local suite**: FULL run green before every push — 1238 passed /
  13 skipped at the #113 tip.
- **CI**: green on both PRs via native `pull_request` runs (the
  base-branch filter removal works; the #105/#108 flake did not
  appear).
- **Bots**: #112 — CodeRabbit CHANGES_REQUESTED round (5 threads) +
  BugBot 1 Medium (shim root anchoring), **all 6 replied and
  resolved**, all accepted with fixes in `2563a4c`. #113 — BugBot
  pass (no findings), CodeRabbit **skipped by policy on
  feature-branch bases** → it will review after retarget; expect one
  more bot round on #113 then.
- **Evaluator trio**: run BEFORE each PR opened (ordering rule).
  2 deep rounds per PR, cap honored. Dispositions:
  `.kit/context/reviews/KIT-0091-evaluator-review.md` — includes two
  empirical refutations recorded as evidence (subprocess
  `capture_output=False, text=True` is legal; GraphQL braces balance,
  verified twice, second time by capturing the actual queries at the
  ghio boundary).

## Parity record (F2) — what makes this trustworthy

Three implementation-parameterized matrices drive the bash original
and the Python port through identical stubs, committed BEFORE each
port:

- preflight: 47 scenarios ×2 (`tests/test_preflight_check.py`)
- review-input: 21 ×2 (`tests/test_prepare_review_input.py`)
- gh-helper: 20 ×2 (`tests/test_gh_review_helper.py`)
- worktree: the pre-existing `tests/test_new_worktree.py` contract
  runs the delegator end-to-end (git-2.30 stubs included)

Each PR's middle commit is the parity proof (both implementations
green with the bash still untouched); the shim commit then proves the
script contract survives delegation. `#113`'s own review input was
generated by the freshly-shimmed script — first production run.

## What a human reviewer should actually look at

1. **Documented divergences** (each in the PR bodies): Gate 3
   all-green rule for multi check-runs; in-package bots-declaration
   reading; sorted Gate-5 pick; poll-delay seam; jq-binary drop
   (#112) — NUL-probe binary detection; in-process stderr capture;
   strict helper slug validation; logical-path primary resolution
   (#113). Everything else is matrix-pinned bash behavior.
2. **`ghio` boundary rules** (`ghio.py`): no env scrubbing (unlike
   gitio) — deliberate, documented; `--repo` position is part of the
   pinned command shape.
3. **F5 refspec WARN** (`55-worktree-provisioning.sh`, both copies
   identical): fires from primary AND worktrees via shared config;
   silent without an origin remote.
4. **Shim root anchoring**: all three shims `cd` to their own repo
   root before delegating (BugBot #112 finding) while `agentive`
   CLI keeps CWD discovery — two entry points, two documented
   discovery rules.

## Release

`agentive-kit-v0.2.0` tag AFTER both PRs merge (existing
trusted-publishing workflow). Version already bumped in-package;
single-sourced.

## Post-merge follow-ups (phase 2 pointers, not blockers)

- wait-for-bots/check-bots stay bash (thin wrappers; spec'd out of
  scope unless trivially absorbed — they weren't).
- new-worktree.sh door surface + any CLI exposure of
  `agentive_kit.worktree` = phase 2 door decision.
- One-release deprecation: remove the three shim bodies in 0.3.x.
