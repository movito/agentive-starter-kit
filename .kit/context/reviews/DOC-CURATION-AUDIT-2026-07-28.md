# Doc-Curation Audit — 2026-07-28

**Run**: planner-f5 orchestrated workflow (5 area readers + README
analyst + adversarial verifiers on every non-keep disposition; 10
agents, ~650k tokens). Against post-KIT-0067 main (`0294bc3`).
Operator brief: curation, not truth — KIT-0069 handled truth; this
asks audience/currency/redundancy/load-bearing per doc.

**Totals**: 33 docs — 21 keep, 6 trim, 1 merge, 5 archive. Verifier challenges: 0.

## Non-keep dispositions (operator sign-off required)

### [TRIM] `docs/MANIFEST-UPGRADE-GUIDE.md` (293L)
**Audience**: split: downstream repo maintainers doing the one-time v1.x→v2.0.0 migration (ADV/DSP/AEL still owe it) + agents citing the evergreen Agent Model Pins and pull-sync sections
**Rationale**: Load-bearing (upgrader agent refuses scripts work by pointing here; all four planner/fd agents cite its model-pin step; KIT-0059 will edit it) but its spine is a dated April one-time migration procedure whose 50-line hardcoded example manifest is exactly the drift class KIT-0069/A57 flagged — cut: replace the full inline example manifest in Step 2 with a pointer to the live scripts/.core-manifest.json plus the opted_in delta, and compress Steps 4-6 (manual command copying, VERSION diffing, source-field cleanup) which project sync now subsumes; keep the tier table, the pull-based sync section, and Agent Model Pins verbatim since those are what live surfaces cite.
**Citations**: .claude/agents/upgrader.md:56,422; .claude/agents/planner.md:32; planner-f5.md:36; feature-developer.md:28; feature-developer-f5.md:33; .kit/tasks/1-backlog/KIT-0059-remove-skills-read-both-symlinks.md:44; .kit/context/KIT-0057-skills-home-inventory.md:38; docs/DISTRIBUTION-ARCHITECTURE.md:9,131,292,301; docs/PLUGIN-UPGRADE-GUIDE.md:19
**Verifier (agree)**: Trim is safe and well-aimed: re-ran the citation grep and all live citers point at sections the trim keeps — the four agent files (planner.md:32, planner-f5.md:36, feature-developer.md:28, feature-developer-f5.md:33) cite specifically the model-pin step (Agent Model Pins section, kept verbatim), upgrader.md:56,422 / DISTRIBUTION-ARCHITECTURE.md:9,131,292,301 / PLUGIN-UPGRADE-GUIDE.md:19 cite the doc as the scripts-surface generally. The pointer target scripts/.core-manifest.json exists, is tiered, and independently proves the drift: the doc's inline Step 2 example is frozen at core_version 2.0.0 with 14 scripts_core entries vs the live 3.9.0 with 27 (missing all doctor.d/*, lib/target_repo.sh, prepare-review-input.sh, check_cross_repo_config.py). Steps 4-6 are subsumed by the pull-sync section the doc itself contains ('Or wait for the next sync run'). One coordination requirement: KIT-0059-remove-skills-read-both-symlinks.md:44 plans to update the example manifest and lines 144-147 carry the KIT-0057 deprecation note — replacing the example with a pointer satisfies KIT-0059's item early, but that checklist line and the deprecation note must be adjusted in the same change.

### [ARCHIVE] `docs/prd/.gitkeep` (0L)
**Audience**: nobody — the directory is empty (single zero-byte .gitkeep from Nov 2025) and represents a PRD convention that never materialized
**Rationale**: Zero citations anywhere in the repo (grep -Rn 'docs/prd' returns nothing outside historical dirs), no content, no audience — delete the empty scaffold rather than let it imply a PRD workflow the kit does not have.
**Citations**: none — grep -Rn 'docs/prd' across the repo (excluding .git, archive, retros, reviews, 5-done, 6-canceled, .kit/adr, CHANGELOG) returns zero hits
**Verifier (agree)**: Archive/delete is right but the 'zero citations anywhere' evidence is incomplete: the directory is a single zero-byte .gitkeep (Nov 2025) and the literal grep for 'docs/prd' returns nothing live, but a case-insensitive word grep finds README.md:411 advertising 'prd/ # Product Requirements Documents' in the directory-structure tree (missed because it appears as a tree entry, not the literal path). This doesn't rescue the scaffold — an empty directory advertised in README is worse than an unadvertised one — but the deletion must also remove the README.md:411 tree line or the README misrepresents the layout.

### [ARCHIVE -> docs/archive/] `.kit/docs/TESTING.md` (192L)
**Audience**: nobody specific — generic pytest/TDD tutorial; agents and CLAUDE.md route to TESTING-WORKFLOW.md instead
**Rationale**: Fully redundant with .kit/context/workflows/TESTING-WORKFLOW.md (362 lines, the surface CLAUDE.md's workflow table actually cites), which covers every topic here — slow markers, SKIP_TESTS, Red-Green-Refactor, test_template.py, coverage; this copy is also stale in details (says 'source venv/bin/activate' where the kit uses .venv; 'Overall >70%' vs the pyproject fail_under 80 rule) and untouched since 2026-03-30, i.e. pre-ADR-0027 framing with zero live citations.
**Citations**: grep -Rn 'TESTING.md' (excluding TESTING-WORKFLOW hits) → only historical ASK-0044-HANDOFF:77,135 and a comment in scripts/local/engine-materials.sh:104 that says 'the old TESTING.md no longer exist(s)' — the one live mention actively believes the doc is already gone.
**Verifier (agree)**: Re-ran grep -Rn 'TESTING\.md' repo-wide: after excluding historical records (KIT-ADR-0023, KIT-0069 retro/review, the stale ASK-0044 handoff), the only live hit is scripts/local/engine-materials.sh:104 — a comment about the OLD docs/TESTING.md path no longer existing, not a citation of this doc. Content comparison confirms full redundancy: TESTING-WORKFLOW.md (the surface CLAUDE.md's workflow table actually cites, and which also ships to consumers via the same .kit/ rsync) covers slow markers, SKIP_TESTS, Red-Green-Refactor, test_template.py, AAA, and coverage, plus newer KIT-0048 fixture-scope material TESTING.md lacks. Staleness verified: 'source venv/bin/activate' (kit uses .venv) and '>70% overall' vs pyproject fail_under=80; untouched since 2026-03-30. Archiving strands no citer and loses no unique content. Minor correction to the reader's evidence only: the engine-materials.sh comment refers to the old path being gone, not the doc entirely — and the .kit/ rsync in that script currently ships this redundant file to exports, so archiving also cleans the export surface.

### [TRIM] `.kit/context/workflows/ADR-CREATION-WORKFLOW.md` (216L)
**Audience**: implementing agent (creating ADRs); planner
**Rationale**: Load-bearing via both agent templates and updated for the KIT-0067 D5 two-homes indexing, but carries source-project relics: the embedded ADR template references the dead task layout `.kit/tasks/active/TASK-YYYY-####.md`, the '300+ lines average' target encourages bloat, and the Best Practices 'Update index (README.md)' line contradicts the correct D5 index section below it — cut those three.
**Citations**: .kit/templates/OPERATIONAL-RULES.md:125, .kit/templates/AGENT-TEMPLATE.md:258, .kit/context/README.md:31, cross-ref from TASK-COMPLETION-PROTOCOL.md:216
**Verifier (agree)**: All three relic claims verified at exact lines: `.kit/tasks/active/TASK-YYYY-####.md` (line 126), 'Average: 300+ lines' (line 155), and Best Practices 'Update index (README.md)' (line 168) which directly contradicts the correct KIT-0067 D5 two-homes section below it (lines 188-193: .kit/adr/about-kit-adr.md vs docs/adr/about-adr.md). Live citations confirmed via grep -Rn: .kit/templates/OPERATIONAL-RULES.md:125, .kit/templates/AGENT-TEMPLATE.md:258, .kit/context/README.md:31 — so trim, not archive, is right. One citation error that doesn't change the verdict: the fourth citation is reversed — ADR-CREATION-WORKFLOW.md:216 cites TASK-COMPLETION-PROTOCOL, not the other way around; TASK-COMPLETION-PROTOCOL.md contains no reference to ADR-CREATION.

### [TRIM] `.kit/context/workflows/AGENT-CREATION-WORKFLOW.md` (902L)
**Audience**: kit developer / operator; agent-creator agent reads it as instructions
**Rationale**: Genuinely load-bearing (agent-creator agent cites it three times as 'the comprehensive guide') but 902 lines is 3-4x its useful weight: the ~190-line Complete Example Walkthrough (lines 414-602) restates the 13 steps verbatim with the same api-tester content, and the Verification Checklist + Troubleshooting sections re-repeat Best Practices — cut the walkthrough and dedupe to roughly 400 lines, keeping steps, the Evaluator Instructions Standard, and the script section.
**Citations**: .claude/agents/agent-creator.md:244,333,346, .kit/context/README.md:30 (self-review SKILL.md:114 mentions are incidental gotcha examples, not instruction pointers)
**Verifier (agree)**: Duplication verified by heading map: Steps 1-13 (lines 82-374) and the Complete Example Walkthrough (414-602) cover the identical 13-step sequence with the same api-tester content (Customize Frontmatter, Define Core Responsibilities, Register, Test, Commit all appear twice). Citations confirmed: .claude/agents/agent-creator.md:244,333,346 and .kit/context/README.md:30; the self-review SKILL.md:114 hits are indeed historical gotcha anecdotes (KIT-0065/0069 line references), not instruction pointers — though a heavy trim will invalidate the ':423 and :442' line numbers cited there, which is acceptable since they are anecdotes. One caveat on scope: the claim that Troubleshooting 're-repeats Best Practices' is overstated — lines 747-887 contain unique diagnostic content (YAML syntax errors, leftover placeholders, evaluator contradictions) not present in Best Practices; the trim should target the walkthrough and Verification Checklist and condense (not delete) Troubleshooting.

### [TRIM] `.kit/context/workflows/COMMIT-PROTOCOL.md` (404L)
**Audience**: implementing agent (all agents that commit)
**Rationale**: The most-cited workflow doc (CLAUDE.md table, six agent files, commit-push-pr command, both templates) and its KIT-0048/0053/0035 retro rules are live doctrine, but it still recommends `alias gpush="./scripts/core/ci-check.sh && git push origin main"` (twice) and an 'Updated Git Workflow' block pushing directly to main — direct-to-main contradicts the kit's feature-branch + PR workflow; cut the alias blocks and compress the 50-line interactive CI-fix example dialogue.
**Citations**: CLAUDE.md:84, .claude/agents/test-runner.md:190, security-reviewer.md:145, agent-creator.md:462, powertest-runner.md:383,387, document-reviewer.md:215, .claude/commands/commit-push-pr.md:57, .kit/templates/AGENT-TEMPLATE.md:306, .kit/tasks/9-reference/templates/task-template.md:310, .kit/docs/LINEAR-SYNC-BEHAVIOR.md:164
**Verifier (agree)**: All claims verified: `alias gpush="./scripts/core/ci-check.sh && git push origin main"` at lines 148 and 153, and the 'Updated Git Workflow' section (line 272) whose MANDATORY 'after' path still runs `git push origin main` (lines 278, 286) — contradicting the kit's feature/<TASK-ID> branch + PR policy in CLAUDE.md. Heavy live citation load confirmed (CLAUDE.md:84, six agent files, commit-push-pr.md:57, AGENT-TEMPLATE.md:306, task-template.md:310, LINEAR-SYNC-BEHAVIOR.md:164), so trim not archive. Strengthening evidence the reader missed: the identical gpush alias + push-to-main block also appears in TESTING-WORKFLOW.md lines 253-268 ('Usage: git push origin main' + 'Recommended alias') — the same cut should be applied there in the same pass or the contradiction survives in the sibling doc.

### [ARCHIVE] `.kit/context/workflows/RESEARCH-QUALITY-STANDARDS.md` (206L)
**Audience**: nobody current — written for research/knowledge-producing agents this kit does not ship
**Rationale**: Pre-transformation (v1.0.0, 2025-01-28, never revised) generic research-citation standards whose only citation sits inside an optional 'delete this section for implementation-focused agents' block of AGENT-TEMPLATE.md; no agent in .claude/agents/ produces research documents, so it serves no live audience — archive to docs/archive/ and drop or footnote the optional template section.
**Citations**: .kit/templates/AGENT-TEMPLATE.md:211 only (inside the delete-if-unused research section, lines 208-211)
**Verifier (agree)**: Grep -Rn over the whole repo (historical dirs excluded) finds exactly one live citation: .kit/templates/AGENT-TEMPLATE.md:211, and I verified it sits inside the optional block headed by 'Delete this section for implementation-focused agents' (lines 206-211). A grep for the doc's actual content ('Research Quality', 'Four Quality Gates', 'Citation Integrity') across .claude/agents/, .claude/skills/, and .claude/commands/ returns zero hits — no shipped agent is knowledge-focused. Archive strands nothing as long as the companion edit to AGENT-TEMPLATE.md (drop or footnote the optional section) lands in the same change, which the reader's disposition already includes.

### [TRIM] `.kit/context/workflows/TASK-COMPLETION-PROTOCOL.md` (128L)
**Audience**: implementing agent at task end — but partially superseded by the preflight command's 7 gates and the wrap-up skill
**Rationale**: Still linked from CLAUDE.md and the only home of the handoff-document format, but its checklist is the informal ancestor of the preflight command's enforced 7 gates and it retains coordinator-era framing ('Notify coordinator', 'XXX/350 tests passing') from the source project — trim to the handoff format + a pointer to preflight/wrap-up as the enforced completion path.
**Citations**: CLAUDE.md:91, COMMIT-PROTOCOL.md:404, TEST-SUITE-WORKFLOW.md:165, ADR-CREATION-WORKFLOW.md:216 (no agent or skill cites it directly; .claude/commands/preflight.md gates cover the same ground mechanically)
**Verifier (agree)**: Facts verified: only live citations are CLAUDE.md:91 plus three sibling-workflow cross-refs (no agent, skill, or command cites it); coordinator-era framing confirmed ('Notify coordinator' step 8 line 87, 'XXX/350 tests passing' lines 51-52, agent-handoffs.json steps 4-6); it is the only home of the handoff-document format, which IS live (feature-developer.md:158,518 and feature-developer-f5.md consume .kit/context/<TASK-ID>-HANDOFF-*.md files); the proposed pointer targets exist (.claude/commands/preflight.md with 7 gates, .claude/commands/wrap-up.md). One correction to the rationale that should shape the trim: preflight's 7 gates cover PR-readiness (CI, two bots, threads, evaluator, review starter, task folder) — they do NOT mechanically cover the checklist's 'deliverables implemented', 'documentation updated', or 'no regressions' items, so 'covers the same ground' is overstated; the trim should keep the short checklist (minus coordinator/350-count relics) alongside the handoff format rather than reduce to format-plus-pointer only.

### [MERGE-INTO -> .kit/context/workflows/TESTING-WORKFLOW.md] `.kit/context/workflows/TEST-SUITE-WORKFLOW.md` (165L)
**Audience**: nominally test-runner — but test-runner.md actually cites TESTING-WORKFLOW, not this
**Rationale**: No agent or skill cites it (only sibling workflow cross-refs and one AGENT-CREATION example line); its command list duplicates TESTING-WORKFLOW's, and its quality-threshold table ('≥90% pass rate = acceptable', 43 xfailed, 85.1% baseline) is source-project framing that contradicts actual kit policy where regressions block merge and the suite passes fully — fold the few unique bits (--durations, --lf triage steps, the results-document template) into TESTING-WORKFLOW and retire the thresholds.
**Citations**: TESTING-WORKFLOW.md:360, COVERAGE-WORKFLOW.md:51, AGENT-CREATION-WORKFLOW.md:246 — nothing outside .kit/context/workflows/ except that one template-example line; grep of .claude/agents+skills+commands finds zero references
**Verifier (agree)**: Citation claim fully verified: grep -Rn finds nothing outside .kit/context/workflows/ except AGENT-CREATION-WORKFLOW.md:246 (plus :526, both example listing lines) — zero hits in .claude/agents/, skills/, or commands/; test-runner.md:26 cites TESTING-WORKFLOW.md instead, exactly as claimed. The threshold table is confirmed source-project framing and is even internally inconsistent: it blesses a 298/350 (85.1%) baseline as normal while its own table rates <90% as 'Needs Work', and '≥90% Acceptable' contradicts kit policy (regressions block merge; suite passes fully). The merge accounting is correct: TESTING-WORKFLOW already has --lf (line 43), -n auto (46), -vv -s (40), but lacks --durations=10, the baseline/regression triage steps, and the results-document template, so those must MOVE (also note tests/results/ referenced at line 55 does not exist — drop that path during the move). No live citer is stranded.

### [TRIM] `.kit/context/workflows/TESTING-WORKFLOW.md` (362L)
**Audience**: implementing agent (feature-developer, test-runner) — also cited as authority by production test code
**Rationale**: The most load-bearing workflow doc in the set — cited from tests/conftest.py, two test modules, the test-runner agent, CLAUDE.md, and both templates, and its Known Gotchas (GIT_DIR leak, module-skip trap, fixture-scope caveat) are live institutional memory — but the Pre-Commit Enforcement section names two slow tests that do not exist in this repo (test_error_handling_cascade, test_check_resolve_connection_failure: grep tests/ finds neither) plus '431/433 tests' counts; trim those source-project relics and absorb TEST-SUITE-WORKFLOW's unique content per the merge above.
**Citations**: tests/conftest.py:58, tests/test_preflight_check.py:20, tests/test_project_script.py:31, .claude/agents/test-runner.md:26, CLAUDE.md:85, .kit/templates/AGENT-TEMPLATE.md:194, .kit/tasks/9-reference/templates/task-template.md:309, .kit/context/README.md:29, REVIEW-INSIGHTS.md:127,140, live handoffs KIT-0046/0050/0057
**Verifier (agree)**: Both relic claims verified: neither test_error_handling_cascade nor test_check_resolve_connection_failure exists anywhere in tests/ (grep confirms; the actual @pytest.mark.slow tests live in tests/test_entrance_shims.py and tests/integration/), and the '431/433 tests' + '2 slow integration tests (10s, 4s)' counts at lines 102-104 are stale with them. All cited live surfaces confirmed: tests/conftest.py:58, tests/test_preflight_check.py:20, tests/test_project_script.py:31, .claude/agents/test-runner.md:26, CLAUDE.md:85, AGENT-TEMPLATE.md:194, task-template.md:309, .kit/context/README.md:29 — clearly keep-and-trim, and the Known Gotchas section (GIT_DIR leak, module-skip trap, fixture-scope caveat, KIT-0050 hook contract) must survive since production tests cite it as authority. Addition the reader missed: lines 253-268 contain the same `git push origin main` usage and gpush alias they flagged in COMMIT-PROTOCOL — cut it here in the same pass.

### [ARCHIVE -> delete the stub after repointing citers to .claude/skills/code-review-evaluator/SKILL.md] `.adversarial/docs/EVALUATION-WORKFLOW.md` (1L)
**Audience**: nobody directly — a redirect stub for readers following stale links (agents reading AGENT-TEMPLATE / create-project)
**Rationale**: The merge-into-skill question is already settled: KIT-0067 (D2/A68) archived the full doc to docs/archive/ and left this 1-line tombstone pointing at the code-review-evaluator skill, so the only remaining work is to repoint the three live citers to the skill and remove the stub — it should not become a permanent redirect that keeps stale links alive.
**Citations**: grep -Rn EVALUATION-WORKFLOW (live surfaces only): .kit/templates/AGENT-TEMPLATE.md:101,197 ('Complete Guide' pointers), .claude/agents/create-project.md:161, .kit/tasks/1-backlog/ASK-0050-evaluator-library-as-default.md:47,62,80,102 (backlog spec that plans to 'update EVALUATION-WORKFLOW.md' — needs rescoping to the skill). All other hits are historical (8-archive tasks, KIT-0065/0067/0069 handoffs/review starters).
**Verifier (agree)**: Independently reproduced the citation set exactly (AGENT-TEMPLATE.md:101,197; create-project.md:161; ASK-0050 backlog:47,62,80,102 — all other hits historical). The stub is a pure tombstone; the full doc already lives in docs/archive/ and the skill covers current guidance, so nothing is lost. One additional live hit the proposer omitted, README.md:384, already points at the skill + archive path and needs no repointing. .adversarial/docs/ is a whole-dir sync glob (core-manifest.json:56) so deletion prunes downstream automatically, and AGENT-TEMPLATE ships from the kit so the repoint propagates with it — no citer is stranded provided the stated repoint-first sequencing is followed.

### [ARCHIVE -> docs/archive/ (and drop the arch-assess entry from the brace-glob at .claude/agents/create-project.md:162; it leaves the manifest payload automatically via the .adversarial/templates/ dir glob)] `.adversarial/templates/arch-assess-input-template.md` (78L)
**Audience**: nominally planner/operator preparing arch-review inputs; in practice nobody — no instruction tells anyone to use it
**Rationale**: Zero instructional citers and practice has diverged: planner/planner-f5 run arch-review(-fast) directly on task/ADR files (planner.md:175-178, planner-f5.md:179-182), and the real recent arch inputs (.adversarial/inputs/KIT-ADR-0025/0026-arch-input.md) are plain ADR content, not this template's module-API/dependency-graph structure — pre-ADR-0027 code-arch-assessment framing from the ASK-0022/0024 era (file untouched since Mar 30).
**Citations**: grep -Rn arch-assess-input-template: no hits outside historical dirs. grep -Rn 'adversarial/templates': only .claude/agents/create-project.md:162 (file-inventory brace-glob, not instruction) and scripts/.core-manifest.json:57 / docs/MANIFEST-UPGRADE-GUIDE.md:133 (whole-dir sync glob). No skill, command, or agent tells anyone to fill it in.
**Verifier (agree)**: Grep confirms zero instructional citers: only create-project.md:162 (file-inventory brace-glob) plus whole-dir sync globs (core-manifest.json:57, MANIFEST-UPGRADE-GUIDE.md:133). The asymmetry is decisive evidence — both sibling templates are cited by live instruction surfaces (check-spec.md:31 for spec-compliance, code-review-evaluator SKILL.md:123 for code-review) while arch-assess is cited by none. Verified planner.md/planner-f5.md run arch-review(-fast) directly on task/ADR files, and KIT-ADR-0025-arch-input.md is plain ADR content with none of the template's module-API/dependency-graph structure; file untouched since a Mar 30 path-move commit. Archiving to docs/archive preserves the structure, and the brace-glob edit in create-project.md is the only live surface needing an update.

## Keeps (21)

- `docs/STARTING-A-PROJECT.md` (189L) — The 4-day-old canonical operator flow for the post-ADR-0027 world (factory model, one door, preset, /new-project, LAUNCH convention) — it is the doc the audit told us to judge othe
- `docs/CROSS-REPO-PATTERN.md` (430L) — The most load-bearing doc in scope — four agent definitions, project-intake, upgrader, the code-review-evaluator skill (which mandates staying in sync with it), engine-consumer.sh
- `docs/DISTRIBUTION-ARCHITECTURE.md` (340L) — The only system map of the two distribution channels and the agents-are-special asymmetry; fully current (v1.2.0 stamped 2026-07-27: parked-push KIT-0045 notes, canonical-homes P6
- `docs/PLUGIN-UPGRADE-GUIDE.md` (188L) — An agent executes this doc step-for-step and a doctor check (50-plugin-source.sh) reuses its hardened marketplace-source grep, so every line is load-bearing instruction, current (v
- `docs/preset.example` (68L) — Fully post-transformation (relocated by KIT-0058 to the visible <kit-parent>/agentive-config home, ADR-0027 P7) and directly referenced by the door itself as the format reference,
- `docs/adr/about-adr.md` (109L) — The seeded per-project ADR index/how-to that the live ADR-CREATION-WORKFLOW instructs agents to update and a test asserts exists, cleanly separating project ADRs (docs/adr/) from i
- `.kit/docs/KIT-MIGRATION-PLAYBOOK.md` (522L) — Not superseded by the door + sync engine — the door serves NEW projects while this is the only recipe for migrating the three existing pre-.kit downstream repos (AEL ~85 refs, ADV
- `.kit/docs/LINEAR-SYNC-BEHAVIOR.md` (164L) — Zero live citations, but it is the sole reference for the opt-in Linear sync feature (folder-to-status map, three-level status priority, legacy migration table, and the load-bearin
- `.kit/docs/UPGRADE-0.4.0.md` (243L) — Version-specific and citation-orphaned, but it is the only step-by-step recipe for the flat-scripts → core/local/optional restructure that the migration playbook's Prerequisites se
- `.kit/context/workflows/COVERAGE-WORKFLOW.md` (52L) — The model curated doc: rewritten post-KIT-0067/0069 to defer to pyproject.toml as the single source of the number, 52 tight lines, cited from CLAUDE.md and both testing siblings —
- `.kit/context/workflows/EVALUATOR-LIBRARY-WORKFLOW.md` (105L) — ZERO inbound citations anywhere in the repo (the only workflow doc with none) — yet it is the sole home for a real recurring operator procedure (library install/upgrade, the --forc
- `.kit/context/workflows/PR-SIZE-WORKFLOW.md` (160L) — Current through the fresh KIT-0067 archival-PR rules, carries unique evidence tables and the structural-migration override, and is cited from CLAUDE.md plus two sibling workflows a
- `.kit/context/workflows/REVIEW-FIX-WORKFLOW.md` (135L) — Untouched since March but still the canonical CHANGES_REQUESTED loop (max-2-rounds per KIT-ADR-0014, task-stays-in-4-in-review rule) that the live review-handoff skill points to as
- `.kit/context/workflows/STACKED-PR-WORKFLOW.md` (144L) — Hard-won and freshly current — the KIT-0067 corrections block (reopen does NOT trigger CI, auto-retarget only on branch deletion, BugBot-scans-under-skipping) captures a near-miss
- `.kit/context/workflows/TEMP-THEN-COMMIT-PATTERN.md` (89L) — Tight, current (live-implementation pointer updated for KIT-0053's engine-consumer.sh move, with the 0.9.0 shim-removal note), single-purpose defensive pattern cited from CLAUDE.md
- `.kit/context/workflows/WORKFLOW-FREEZE-POLICY.md` (35L) — 35 lines, zero staleness, a rule agents genuinely need mid-task (don't edit skills/commands/agents/workflows during feature work, with the two exceptions), cited from the CLAUDE.md
- `.kit/context/workflows/WORKTREE-WORKFLOW.md` (254L) — The most operationally load-bearing doc in scope and the most current (KIT-0071, 2026-07-27): cited by executable surfaces (new-worktree.sh three times, doctor.d/55 and /70), the u
- `.adversarial/templates/code-review-input-template.md` (65L) — Load-bearing and current: the live code-review-evaluator skill explicitly directs agents to this template for the manual input path, and its content (full-file-not-diff rule, bot-f
- `.adversarial/templates/spec-compliance-input-template.md` (44L) — Load-bearing and actively maintained (modified 2026-07-27): /check-spec uses it as the assembly structure for .adversarial/inputs/<TASK-ID>-spec-compliance-input.md, and backlog KI
- `CLAUDE.md` (111L) — Already trimmed to a lean 111 lines by KIT-0069 (stale launchers/ and retired scripts gone; one-door bootstrap row present), every remaining section maps to a live action surface a
- `CHANGELOG.md` (746L) — Sole release-history surface (Keep-a-Changelog standard), fully current — [Unreleased] documents the entire ADR-0027 arc through KIT-0065/0068 and embeds the KIT-0069/A66 tag-gap n

## README plan (580 -> ~120 lines)

| Section | Verdict | Destination |
|---|---|---|
| Title + intro pitch (lines 1-9) | stay (trim) | Stays in README — the 60-second pitch, harness link, and the pointer to docs/STARTING-A-PROJECT.md. CONSTRAINT: line 1 '# Agentive Starter Kit' is an identity-seeding anchor (ASK-0036 line-1-only replacement; engine-export.sh:262) — the H1 must not change. |
| What's inside? (lines 12-29) | stay (trim) | Stays in README, compressed from 8 numbered paragraphs to a ~8-line bullet list; this is the core 'what do I get' glance for newcomers. |
| Requirements — three tables + doctor blurb (lines 32-72) | move | STARTING-A-PROJECT (which currently has NO prerequisites section — a real gap). README keeps one line: 'You need Claude Code, git+gh, and Python 3.10-3.12 for code projects; project doctor tells you what's missing.' |
| Quick Start — Option A: from a URL (lines 79, 83-106) | move | STARTING-A-PROJECT, as a short 'don't have a clone yet?' alternate path (it's the only pre-clone flow and appears nowhere else; flow was validated per project_create_project_url_flow). The 'honest caveat' paragraph moves with it. |
| Quick Start — Option B: create-project agent (lines 80, 108-142) | move | STARTING-A-PROJECT, condensed to a few lines under an 'other ways to run the door' note — the create-project agent file itself is the instruction surface; the 30-line step-by-step in README duplicates it. |
| Quick Start — Option C: /new-project (lines 81, 144-168) | drop | Delete as redundant — this IS STARTING-A-PROJECT's 'Blank split pair' + 'Single repo' sections almost verbatim. README's single quickstart block points at /new-project and STARTING-A-PROJECT instead. |
| What's Included — Agents table (lines 173-193) | stay (trim) | Stays in README trimmed to ~6 headline agents + the existing 'ls .claude/agents/ is the authoritative inventory' line; the 14-row table is inventory, and the doc itself admits the dir listing is authoritative. |
| What's Included — Task Management folder tree (lines 195-210) | drop | Delete as redundant — the tree is duplicated in CLAUDE.md's Directory Structure and fully documented in .kit/tasks/README.md (which also has the status mapping). One pointer line in README's pointers section. |
| What's Included — Adversarial Evaluation (lines 212-233) | stay (trim) | Stays in README as 2-3 lines (what it is + 'adversarial list-evaluators' + install command); the per-command walkthrough duplicates the CLI's own help and the code-review-evaluator skill. |
| What's Included — Serena Integration (lines 236-246) | stay (trim) | Stays as one line in the What's-inside list (token-saving semantic navigation); LSP language list and the dashboard-popup note drop — .serena/ config and the note serve nobody on the front page. |
| Configuration (lines 250-261) | drop | Delete as redundant/outdated — post-ADR-0027 the door + preset ARE configuration; the manual template-copy list is pre-transformation framing. The .env/.serena/.adversarial template facts live with the door and doctor. |
| Linear Integration (lines 264-329, ~66 lines) | move | New reference page docs/LINEAR-INTEGRATION.md — this is the ONLY home of the full setup steps (team creation, API key, GitHub Actions secrets); .kit/tasks/README.md has only the status mapping, so it cannot simply be deleted. README keeps one pointer line. The new page can absorb .kit/tasks/README.md's mapping table by reference. |
| Usage — Launching Agents / Creating Tasks / Running Tests (lines 333-374) | drop | Delete as redundant — 'claude --agent' launching and the first-session flow are in STARTING-A-PROJECT ('Your first session', LAUNCH convention); task creation belongs to the planner + .kit/tasks/README.md; 'pytest tests/ -v' is generic and in CLAUDE.md/TESTING-WORKFLOW. |
| Documentation links (lines 378-387) | stay | Stays in README — becomes the seed of the new compact Pointers section (dedup against the outline's pointer list; the 'archived' entries can drop to keep it live-surfaces-only). |
| Project Structure tree (lines 390-417) | drop | Delete as redundant — near-duplicate of CLAUDE.md's Directory Structure (the surface agents actually read, per CLAUDE.md:5's own framing); a newcomer with 60 seconds doesn't need a 25-line tree. |
| Philosophy (lines 421-442) | stay (trim) | Stays in README compressed to ~6 lines — it's the kit's identity statement and exists nowhere else; the four-heading format is padding. |
| Four Ways — 'From a URL' / 'Agent-Driven' / 'Guided Interview' subsections (lines 445-471) | drop | Delete as redundant — pure internal duplication of Quick Start Options A/B/C (each subsection literally links back to its Quick Start twin). |
| Four Ways — Consumer Project / --adopt (lines 473-488) | move | STARTING-A-PROJECT, as a fourth way ('Adopt an existing repo') — this is the only creation flow absent from that doc; per that doc's own rule, shape × profile details stay with bootstrap --help. The prototype-graduation paragraph (line 488) is already covered there and drops. |
| Four Ways — Operator Preset rules (lines 490-509) | move | Merge-into STARTING-A-PROJECT's existing 'operator preset' section (condensed rules list: never distributed, guardrails, secrets by reference, records beat presets, malformed fails loud) — README's 20-line rules block is operator reference, wrong altitude for the front page; /setup-preset is the doer. |
| Four Ways — Degraded Modes (lines 511-517) | move | Merge-into STARTING-A-PROJECT (short note beside the adopt path): --bots declaration → Gates 2/3 SKIP; bot-app installs are an operator step; single-key mode lives in the code-review-evaluator skill. Flag detail stays with bootstrap --help. |
| Pulling Updates from the Starter Kit (lines 521-553) | move | A docs/ reference page — docs/UPDATING-YOUR-PROJECT.md (sited alongside MANIFEST-UPGRADE-GUIDE.md / PLUGIN-UPGRADE-GUIDE.md, which already own the upgrade story) — and reconcile with 'project sync' (sync engine 3.0.0), since whole-repo upstream merge vs. pull-sync is exactly the kind of framing that needs one page, not a README tail. README keeps one pointer line. |
| Contributing (lines 556-563) | stay | Stays in README — 6 lines, appropriate for a GitHub front page. |
| License (lines 566-568) | stay | Stays in README. |
| Acknowledgments (lines 572-574) | stay (trim) | Stays as one line, folded under Contributing/License. |
| Version footer + KIT-0069/A53 note (lines 578-580) | stay (trim) | Stays as one line ('Version: see pyproject.toml'); the parenthetical about the hand-maintained footer sitting three releases behind is kit-dev archaeology and drops. |

### Proposed outline

PROPOSED NEW README (~120 lines; pointers, not copies)

# Agentive Starter Kit                       [H1 unchanged — identity-seeding anchor, line-1-only replacement in export flows]
3-4 line pitch: structure for agentive development (harness link); stamp projects from one permanent clone in ~10 minutes. One bolded pointer: "Starting a project? Read docs/STARTING-A-PROJECT.md."   (~10 lines)

## What's inside                              (~12 lines)
Compressed bullet list: one setup door + /new-project; specialized agents; adversarial evaluators (second opinions); task management (+ optional Linear); TDD infrastructure; ADRs; Serena (70-98% token savings on navigation). Each bullet one line, each naming its pointer target where one exists.

## Quickstart — ONE path                      (~18 lines)
Prereq one-liner: Claude Code, git + gh (authenticated), Python 3.10-3.12 for code projects — `./scripts/core/project doctor` in any created project tells you what's missing.
```
cd ~/Github && git clone https://github.com/movito/agentive-starter-kit.git
cd agentive-starter-kit && claude
/new-project
```
Two sentences: the command interviews you and drives the one setup door (scripts/local/bootstrap); open the tab the LAUNCH line prints and start with the planner. "Full guide — factory model, prototype graduation, adopting an existing repo, presets: docs/STARTING-A-PROJECT.md. Authoritative option matrix: ./scripts/local/bootstrap --help."

## Pointers                                   (~18 lines, table)
| Starting a project (all paths) | docs/STARTING-A-PROJECT.md |
| Setup-door options (shapes x profiles, --adopt, --bots) | ./scripts/local/bootstrap --help |
| Operator preset | /setup-preset + STARTING-A-PROJECT |
| Split-pair pattern | docs/CROSS-REPO-PATTERN.md |
| Linear sync | docs/LINEAR-INTEGRATION.md (new page, absorbs README's current 66-line section) |
| Keeping a project updated | docs/UPDATING-YOUR-PROJECT.md (new page, absorbs 'Pulling Updates') |
| Agents inventory | ls .claude/agents/ |
| Tasks | .kit/tasks/README.md |
| Evaluation | .claude/skills/code-review-evaluator/SKILL.md |
| Kit ADRs / your ADRs | .kit/adr/ / docs/adr/ |
| Templates | .kit/templates/ |

## Philosophy                                 (~8 lines)
Compressed: progressive refinement over perfectionism; tests before implementation; multi-model second opinions; documentation is infrastructure.

## Contributing / License / Version           (~8 lines)
Contributing 3 lines; MIT; one-line acknowledgment; "Version: see pyproject.toml".

DEPENDENCIES BEFORE THE TRIM CAN LAND (content that must move first, because README is its only live home):
1. Create docs/LINEAR-INTEGRATION.md (full setup steps exist ONLY in README; .kit/tasks/README.md has only the status mapping).
2. Create docs/UPDATING-YOUR-PROJECT.md (or fold into MANIFEST-UPGRADE-GUIDE.md) for the upstream-merge + reconfigure content, reconciled with `project sync`.
3. Add to STARTING-A-PROJECT: prerequisites section, 'Adopt an existing repo' (--adopt) as a fourth way, URL zero-setup + create-project-agent as alternate door entries, condensed preset rules, degraded-modes note.
4. Keep the H1 '# Agentive Starter Kit' byte-identical (seeding anchor) and keep CLAUDE.md:5's 'see README.md' pointer valid — the slim README still satisfies it.

CITATION EVIDENCE SUMMARY: live citations of README = CLAUDE.md:5 (generic 'full details' pointer), engine-export.sh:262 + create-project.md Step 4 (README gets REPLACED in created projects — the kit README never ships downstream), ASK-0036 seeding (line 1 only), tests (file-existence seeds only, content-agnostic beyond line 1). No live surface deep-links any README section anchor, so every section is free to move without breaking a link.
