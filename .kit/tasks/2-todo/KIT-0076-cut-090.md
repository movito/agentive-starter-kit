# KIT-0076: Cut 0.9.0 — execute the pinned removals, release the lean kit

**Status**: Todo
**Priority**: high
**Assigned To**: unassigned
**Estimated Effort**: 0.5-1 day
**Created**: 2026-07-28
**Linear ID**: (automatically backfilled after first sync)

## Related Tasks

**Executes** (the three pinned removal specs are the requirements —
read all three IN FULL; this task only sequences them):
- `.kit/tasks/1-backlog/KIT-0047-remove-verify-setup-shim.md`
- `.kit/tasks/1-backlog/KIT-0054-remove-entrance-shims.md`
- `.kit/tasks/1-backlog/KIT-0059-remove-skills-read-both-symlinks.md`
  (includes the legacy `~/.config/agentive-kit` notice removal added
  by KIT-0058, and note its example-manifest checklist item was
  satisfied early by KIT-0073)

**Related**: KIT-0075 (launcher — NOT part of this release's
removals; `.kit/launchers/launch` is deliberately kept), KIT-0067
retro action (downstream note below)

## Overview

Every deprecation window promised at 0.9.0 closes: the verify-setup
shim, the three setup-door entrance shims, and the `.kit/skills/`
read-both symlinks plus the legacy-config notice. Then the release
mechanics: CHANGELOG cut, version bump, tag. The kit that ships is
the ADR-0027 end state with no compatibility scaffolding left.

## Requirements

- **F1-F3**: execute KIT-0047, KIT-0054, KIT-0059 per their own
  specs (each carries its removal list, guard-test updates, and
  citation-sweep requirements). Move each to `4-in-review` as part
  of this task's PR (they complete together with this task).
- **F4 — removal hygiene** (binding patterns): every deleted surface
  gets the citer sweep WITH variants (item 15) — and per the launch
  lesson, any surface an operator might invoke by hand gets flagged
  to the planner before deletion rather than assumed dead (the three
  removal sets were all deprecation-noticed, so surprises are
  unlikely — but check).
- **F5 — release mechanics**: CHANGELOG `[0.9.0]` cut (the arc since
  0.8.0: PRs #69-#99 territory — summarize by theme: ADR-0027
  transformation, factory front door, remediation chain, curation;
  don't enumerate every PR), compare-link definitions updated
  (KIT-0069 fixed the format — keep it), pyproject 0.8.0 → 0.9.0,
  core scripts VERSION bump if core files change, manifests + count
  tests in the same commit. Tag `v0.9.0` AFTER CI green on the
  merged main — the operator or planner tags, not this task's
  session; note it in the review starter.
- **F6 — downstream note (KIT-0067 retro action)**: add to the
  release notes / CHANGELOG entry a migration line: downstream
  consumers on next sync will have retired surfaces
  deletion-pruned (shims, `.kit/skills` symlinks, launchers'
  onboarding); the downstream pass runs the sync per repo — this
  task only documents it.

## Acceptance Criteria

- [ ] All three removal specs' own acceptance criteria met (each
      checked off in the PR body)
- [ ] Repo-wide variant-sweep greps prove no live citer of any
      removed surface (pasted in PR)
- [ ] CHANGELOG 0.9.0 entry + links; pyproject 0.9.0
- [ ] CI green across the matrix; tag AFTER merge, noted in starter
- [ ] Planner tree-grounded verification requested before merge
      (deletion-heavy diff — evaluators reconstruct pre-fix state;
      fast-only trio for the Gate-5 record per the standing rule)

## Notes

- **Evaluation skipped (planner)**: the removal decisions were made
  and evaluated in their own specs and the ADR; release mechanics
  are convention. Compensating control: tree-grounded verification
  gates the PR.
- The three executed tasks move to 5-done at closeout alongside this
  one.
- Out of scope: the downstream pass itself, KIT-0075, dedup-analysis
  findings (post-0.9.0 theme), KIT-0062/0064/0070/0074 backlog.
